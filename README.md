# NLP_sentiment_analysis
In this project the model is trained to analyze the meaning of a comment whether positive, negative or neutral. DistilBERT is used which uses Transformer-based architecture. The model is enabled to accurately predict without confusion of sarcasm and negation of natural language.
